# Cattle Sprayer Mesh (legacy)

**Deprecated.** Use the house gateway with:

1. **MQTT discovery** (set broker in the gateway Settings), or
2. HACS **Sinewerx Mesh** (`sinewerx_mesh`) over HTTP / mDNS.

This companion-TCP / USB integration fights the standalone gateway dashboard.
